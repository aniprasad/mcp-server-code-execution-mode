# Requirements for MCP servers in this directory
# Install with: pip install -r requirements.txt

mcp>=1.0.0
pydantic>=2.0.0

# Browser automation (for servers/browser.py)
# After installing, run: playwright install chromium
cesail>=0.2.0
playwright>=1.40.0
