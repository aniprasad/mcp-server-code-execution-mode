{
  "mcpServers": {
    "weather": {
      "command": "python",
      "args": ["${workspaceFolder}/servers/weather.py"],
      "description": "Get weather information for cities (current conditions and forecasts)"
    },
    "soccer": {
      "command": "python", 
      "args": ["${workspaceFolder}/servers/soccer.py"],
      "description": "Get live soccer matches, upcoming games, and league standings"
    }
  }
}
