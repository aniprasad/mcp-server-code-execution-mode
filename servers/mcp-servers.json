{
  "mcpServers": {
    "weather": {
      "command": "python",
      "args": ["${workspaceFolder}/servers/weather.py"],
      "description": "Get weather information for cities (current conditions and forecasts)"
    },
    "sports": {
      "command": "python", 
      "args": ["${workspaceFolder}/servers/sports.py"],
      "description": "Get live scores, standings, and schedules for NFL, NBA, MLB, NHL, Soccer, and F1"
    },
    "stocks": {
      "command": "python",
      "args": ["${workspaceFolder}/servers/stocks.py"],
      "description": "Get real-time stock quotes, historical prices, market indices, and cryptocurrency data"
    },
    "fx": {
      "command": "python",
      "args": ["${workspaceFolder}/servers/fx.py"],
      "description": "Convert currencies and get exchange rates (EUR, USD, GBP, JPY, and 30+ currencies)"
    },
    "wikipedia": {
      "command": "python",
      "args": ["${workspaceFolder}/servers/wikipedia.py"],
      "description": "Search Wikipedia and get article summaries, historical events, and random articles"
    },
    "msforms": {
      "command": "python",
      "args": ["${workspaceFolder}/servers/msforms.py"],
      "description": "Fetch Microsoft Forms data (questions, responses, aggregated stats) for analysis with pandas. Owner auto-detected from form ID.",
      "env": {
        "MS_FORMS_TENANT": "YOUR_MS_TENANT_ID",
        "MS_FORMS_USER": "YOUR_MS_FORMS_USER_ID",
        "MS_FORMS_TOKEN": "YOUR_MS_FORMS_TOKEN",
        "MS_FORMS_BEARER": "YOUR_MS_FORMS_BEARER",
        "MS_FORMS_COOKIES": "YOUR_MS_FORMS_COOKIES"
      }
    }
  }
}
